# InfluxDB Token Environment Variables
# HUB_TOKEN will be auto-populated by the setup script. Leave it empty!
HUB_TOKEN=
SERVER_INFLUXDB_URL=http://<SERVER_IP>:8086 # Update with actual server IP
SERVER_TOKEN=<TOKEN> # Update with server influxdb token